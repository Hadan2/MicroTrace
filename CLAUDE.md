# MicroTrace

> 이 파일은 **얇은 포인터**다. 실제 AI 작업 규칙은 `.claude/`에 있고, 아래 import로 자동 로딩된다.
> 문서 전체 지도는 [`docs/README.md`](docs/README.md)를 본다.
>
> Claude Code는 작업 디렉토리에서 위로 올라가며 `CLAUDE.md`를 자동으로 읽는다.
> 마스터 규칙(`.claude/CLAUDE.md`)과 todo를 여기서 `@`로 끌어온다.

@.claude/CLAUDE.md
@docs/ai/todo.md
